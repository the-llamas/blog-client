{{#each posts as |post|}}
<div class="item" data-id="{{post._id}}">
  <img id="img-{{post._id}}">
  <h3>{{post.title}}</h3>
  <p>{{post.text}}</p>

  <div class="btn-div" title="{{post.title}}" text="{{post.text}}" img="{{post.img}}">
    <button class="btn edit-post" data-id="{{post._id}}" user={{post.owner._id}}>EDIT</button>
  </div>
  <div class="btn-div">
    <button class="btn delete-post" data-id="{{post._id}}" user={{post.owner._id}}>DELETE</button>
  </div>
  <div class="btn-div">
    <button class="btn add-comment-to-post" data-id="{{post._id}}">ADD COMMENT</button>
  </div>
  <section class="comments">
    {{#each comment as |comm|}}
    <div class="item" data-id="{{comm._id}}">
      <p>{{comm.text}}</p>
      <div class="btn-div" data-id="{{comm._id}}">
        <button class="btn edit-comment" data-id="{{comm._id}}" text="{{comm.text}}" user={{comm.owner}}>Edit</button>
        <button class="btn delete-comment" data-id="{{comm._id}}" user={{comm.owner}}>Delete</button>
      </div>
    </div>
    {{/each}}
  </section>
</div>

{{/each}}
